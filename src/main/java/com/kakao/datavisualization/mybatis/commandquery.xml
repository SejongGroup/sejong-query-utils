<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.rog/dtd/mybatis-3-mapper.dtd">
	<!-- 
<mapper namespace="com.kakao.datavisualization.mybatis.QueryMapper">
	
	<select id="selectAll" parameterType="Hashmap" resultType="map">
		SELECT * 
		FROM TB_UTIL_MEMBER
	</select>

</mapper>

 -->
 
 <mapper namespace="commandquery">
	
	<select id="commandSelect" parameterType="Hashmap" resultType="map">
		${sql}
	</select>
	
	<insert id="commandInsert" parameterType="Hashmap">
		${sql}
	</insert>
	
	<delete id="commandDelete" parameterType="Hashmap">
		${sql}
	</delete>
	
	<update id="commandUpdate" parameterType="Hashmap">
		${sql}
	</update>
</mapper>